define(["qlik"],
    function (qlik) {

        var percentName = {
            ref: "qDef.SmartWharfPie.percentName",
            label: "比重名称",
            type: "string",
            defaultValue: "份额"
        };

        return {
            // percentName: percentName
            // symbolSize: symbolSize,
            // animation: animation,
            // effectColor: effectColor,
            // maxLineNum: maxLineNum
        };
    });